/**
 *  时间格式
 * @param data -> 时间
 * @param fmt -> 格式化方式（如： yyyy-MM-dd）
 */
export declare function formatDate(data: any, fmt: string): string;
/**
 * 获取当前时间n天之后的日期
 */
export declare function getAfterDate15(n: number): string;
